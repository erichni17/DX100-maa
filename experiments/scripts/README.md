# Experiment Scripts

Place active experiment launch scripts here instead of DX100 repo root.

Legacy root scripts moved during July 2026 cleanup are in:

- `legacy_root_2026-07/` (ignored by git)

Tracked root harness scripts were migrated to:

- `active_root_migrated_2026-07/`

Compatibility wrappers remain at the old root paths and forward to this directory.
